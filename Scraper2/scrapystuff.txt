/////ItemLoader method for items
// within parse

all_ads = response.css('p.result-info')

for ads in all_ads:

  loader = ItemLoader(item=WebPageItem(),
    selector=ads, response=response)

  loader.add_css('title', 'a.result.title.hdrlink::text')

  yield loader.load_item()



/////Using regular item storing


all_ads = response.css('p.result-info')

for ads in all_ads:
  title = ads.css('a.result.title.hdrlink::text').get()

  items = CraigsListItem()
  items['title'] = title

  yield items





/////adding process at the bottom of the script to avoid having to use `scrapy crawl` to run the spider
    /// allows us to just call the name of the script to execute it

process = CrawlerProcess()
process.crawl(`spider class name`)
process.start()

